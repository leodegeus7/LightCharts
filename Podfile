# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'DemoApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DemoApp
  pod 'Charts', '~> 3.2.2'
  target 'DemoAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DemoAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'LightCharts' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Charts', '~> 3.2.2'
  # Pods for LightCharts

  target 'LightChartsTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
